<UserControl x:Class="CryptographyTutor.Content.Tutor.BCiphers._19"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="Strength of DES – Timing Attacks" Style="{StaticResource Heading1}" />
				<TextBlock Text="attacks actual implementation of cipher" />
<TextBlock Text="use knowledge of consequences of" />
<TextBlock Text="implementation to derive information about" />
<TextBlock Text="some/all subkey bits" />
<TextBlock Text="specifically use fact that calculations can take" />
<TextBlock Text="varying times depending on the value of the" />
<TextBlock Text="inputs to it" />
<TextBlock Text="particularly problematic on smartcards" />

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
