<UserControl x:Class="CryptographyTutor.Content.Tutor.CET._3"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel MinWidth="200">
                <TextBlock Text="Symmetric Encryption" Style="{StaticResource Heading2}" />
				<TextBlock Text="Also known as conventional / private-key / single-key"/>
<TextBlock Text="sender and recipient share a common key"/>
<TextBlock Text="all classical encryption algorithms are private key"/>
<TextBlock Text="was the only type prior to invention of public-key in 1970’s"/>
<TextBlock Text="and by far the most widely used"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
